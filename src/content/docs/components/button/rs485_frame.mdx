---
description: "Instructions for setting up RS485 Frame buttons."
title: "RS485 Frame Button"
params:
  seo:
    description: Instructions for setting up RS485 Frame buttons to send key commands on a DLE-framed RS485 (RS-485) bus.
---

The `rs485_frame` button platform sends a key command onto a DLE-framed RS485
bus when pressed. It requires the [RS485 Frame](/components/rs485_frame/) hub to be configured.

## How commands work

Each button corresponds to a **key code** — a 32-bit value that identifies which
physical button was pressed on the original controller panel. When you press the
button in Home Assistant, the device queues the key frame and transmits it during
the next TX gate window (typically the controller's keep-alive frame, around
every 100 ms for Hayward AquaLogic).

The controller receives the key event and decides what happens next. The device
cannot know the outcome until it reads the next LED status frame, which is why
commands are modelled as buttons rather than switches.

### Finding command values

Known command values for Hayward AquaLogic and Jandy AquaLink RS are listed in the
[rs485-frame-examples](https://github.com/b3nj1/rs485-frame-examples) repository
and were confirmed from live bus captures. Each value is controller-wiring-dependent
for AUX outputs — rename and verify against your physical panel.

To find new command values, enable `dump_frames: true` and `sniffer_only: true` on
the hub, press a physical button on the controller panel, and watch for a new frame
type (typically `0x0083` for Hayward wireless) in the ESP log.

## Configuration variables

- **rs485_frame_id** (**Required**, [ID](/guides/configuration-types#id)): The ID of the `rs485_frame` hub.
- **command** (**Required**, hex uint32): The 32-bit key code to send (e.g. `0x00010000`). The encoding is key-format-specific: for Hayward wireless, this is a bitmask where each bit maps to one physical button.

All other options from [Button](/components/button/#base-button-configuration).

## Example

```yaml
button:
  - platform: rs485_frame
    rs485_frame_id: pool
    name: "Pool Lights"
    command: 0x00010000  # confirmed from bus capture

  - platform: rs485_frame
    rs485_frame_id: pool
    name: "Filter"
    command: 0x80000000

  - platform: rs485_frame
    rs485_frame_id: pool
    name: "Pool/Spa Mode"
    command: 0x40000000
```

Use the corresponding LED binary sensors to read the actual resulting state.
LED state lives in a status frame from the controller; decode it in `on_frame:`
on the hub and push to `template` sensors — see the
[Basic usage example](/components/rs485_frame/#basic-usage) for the LED-mask
pattern. For a single LED:

```yaml
rs485_frame:
  id: pool
  on_frame:
    - frame_type: [0x01, 0x02]
      then:
        - lambda: |-
            if (payload.size() < 6) return;
            uint32_t mask = uint32_t(payload[2]) | (uint32_t(payload[3]) << 8) |
                            (uint32_t(payload[4]) << 16) | (uint32_t(payload[5]) << 24);
            id(led_filter).publish_state(bool(mask & (1UL << 5)));

binary_sensor:
  - platform: template
    id: led_filter
    name: "Filter LED"
```
